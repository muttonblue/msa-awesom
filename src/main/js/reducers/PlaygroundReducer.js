import _ from "lodash";

let initial = {
    UI: {

    }
};

const playground = (state = initial, action) => {
    const nextState = Object.assign({}, state);

    switch (action.type) {

        default:
            return state;
    }
};


export default playground;
